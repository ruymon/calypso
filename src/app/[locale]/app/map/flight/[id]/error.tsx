'use client'; // Error components must be Client Components

interface FlightDetailsErrorPageProps { };

export default function FlightDetailsErrorPage({ }: FlightDetailsErrorPageProps) {
  return (
    <h1>Error to fetch...</h1>
  );
};
